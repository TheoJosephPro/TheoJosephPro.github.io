---
title: "<span style='font-size:28px; line-height:1.2; color: Indigo; font-weight:bold; text-align: center;'>Dans quelle mesure les habitudes d’écoute musicale (fréquence et genres écoutés) influencent-elles la santé mentale auto-déclarée chez les individus (anxiété, dépression, insomnie et TOC) ?</span>"
author: "Bastien BARTHÉLÉMY \n\n Illan CALLEGARI \n\n Théo JOSEPH"
lang: "fr"
output:
  html_document:
    toc: yes
    toc_float: yes
    number_section: yes
    theme: spacelab
    df_print: paged
editor_options: 
  chunk_output_type: console
  markdown: 
    wrap: 65
---

```{r, packages, echo = FALSE, message = FALSE, warning=FALSE}
library(tibble)
library(broom)
library(kableExtra)
library(stargazer)
library(tidyr)
library(forcats)
library(tidyverse)
```

```{r, import database, echo = FALSE, warning=FALSE, message=FALSE}
df <- read.table("mxmh_survey_results.csv", header = TRUE, sep = ",")
```

```{r, fonction afficher_regression, echo = FALSE, warning=FALSE, message=FALSE}
afficher_regression <- function(modele, titre = "Résultats de la régression") {
  require(broom)
  require(kableExtra)
  require(dplyr)

  infos <- glance(modele)
  r2_adj <- round(infos$adj.r.squared, 5)

  tab <- tidy(modele) |>
    mutate(
      Significatif = case_when(
        p.value <= 0.001 ~ "***",
        p.value <= 0.01  ~ "**",
        p.value <= 0.05  ~ "*",
        p.value <= 0.1   ~ ".",
        TRUE ~ ""),
      `R² ajusté` = if_else(term == "(Intercept)", as.character(r2_adj), ""))

  tab |>
    kable(digits = 3,
          caption = titre,
          col.names = c("Variable", "Estimate", "Erreur std.", "t", "p-value", "Seuil", "R² ajusté"),
          align = "c") |>
    kable_styling(full_width = FALSE,
                  bootstrap_options = c("striped", "hover", "condensed"),
                  position = "left") |>
    column_spec(7, border_left = TRUE, extra_css = "border-left: 2px solid #bbb;")}
```

<hr style="height:2px; background-color:#333; border:none; margin-top:30px; margin-bottom:30px;">

# [**Introduction**]{style="color: Indigo;"}

La **musique occupe une place centrale** dans notre quotidien. Que ce soit pour se détendre, se concentrer, extérioriser des émotions ou simplement par plaisir, elle est présente à toutes les étapes de la vie.  
Parallèlement, les **troubles psychologiques tels que l’anxiété, la dépression ou les troubles du sommeil sont en constante progression**, notamment chez les jeunes générations. Face à ce constat, une interrogation naturelle émerge : 

> *La musique peut-elle influencer notre santé mentale ?*
> *Plus précisément, dans quelle mesure les habitudes d’écoute musicale (fréquence et genres écoutés) influencent-elles les troubles psychologiques auto-évalués tels que la dépression, l’anxiété, l’insomnie ou les TOC ?*

--------------------------------------------------------------------------------

Notre travail s’est organisé de cette façon :

- Une première partie de **nettoyage et préparation** des données,
- Une partie de statistiques descriptives des variables intéressantes,
- Puis une série de **modèles économétriques** testant le lien entre musique et santé mentale,
- Enfin, une réflexion sur la **validité des résultats**, notamment autour des problèmes d’**endogénéité**.

L’objectif de ce travail n’est pas de prédire parfaitement les troubles psychologiques, mais plutôt d’analyser les **relations potentielles** entre les habitudes d’écoute musicale et la santé mentale, à travers des outils statistiques et économétriques.

```{r filtrage de la database, echo = FALSE, warning=FALSE, message=FALSE}
# Transformation des fréquences musicales en scores numériques
freq_map <- c("Never" = 0, "Rarely" = 1, "Sometimes" = 2, "Very frequently" = 3)

# Appliquer à chaque genre
genres <- c("Rock", "Rap", "R&B", "Jazz", "Classical", "Country", "EDM",
            "Folk", "Gospel", "Hip hop", "K pop", "Latin", "Lofi", "Metal",
            "Pop", "Video game music")

for (genre in genres) {
  col <- paste0("Frequency..", gsub(" ", ".", genre), ".")
  new_col <- paste0("freq_", gsub(" ", "_", tolower(genre)))
  
  if (col %in% colnames(df)) {
    df[[new_col]] <- as.numeric(recode(df[[col]], !!!freq_map))
  } else {message("Colonne non trouvée : ", col)}}

# Colonnes de fréquence qu'on garde
genres_conserves <- c("freq_lofi", "freq_metal", "freq_classical", 
                      "freq_rap", "freq_hip_hop", "freq_pop", "freq_rock")

# Colonnes principales pour analyse mentale + variables explicatives
colonnes_utiles <- c("Hours.per.day", "Anxiety", "Depression", 
                     "Insomnia", "OCD", "Age", "BPM", "Music.effects")

# Création de la database finale
df_final <- df |> 
  select(all_of(colonnes_utiles), all_of(genres_conserves)) |> 
  drop_na()

# Nettoyage des données aberrantes
df_final <- df_final |> 
  filter(Age >= 12, Age <= 100) |> 
  filter(`Hours.per.day` >= 0, `Hours.per.day` <= 12) |> 
  filter(BPM >= 0, BPM <= 220) |> 
  filter(Anxiety >= 0, Anxiety <= 10,
         Depression >= 0, Depression <= 10,
         Insomnia >= 0, Insomnia <= 10,
         OCD >= 0, OCD <= 10)        
```


## [📊 Présentation de la base de données :]{style="color: DarkSlateBlue;"}

La base de données que nous allons étudier a été remplie par des **participants volontaires anonymes** dans le cadre d’un questionnaire en ligne. Nous commencerons donc par un **nettoyage des données**, visant à supprimer les **valeurs aberrantes** et les **valeurs manquantes**. De plus, nous avons choisi de convertir certaines **variables ordonnées**, notamment celles liées à la fréquence d’écoute par genre musical, en **variables quantitatives** afin de pouvoir les intégrer dans nos analyses économétriques.

Notre nouvelle base de données comporte les variables suivantes :

-  *Hours.per.day* : Nombre d'heures d'écoute par jour ;
-  *Anxiety* : Niveau auto-évalué d'anxiété sur une échelle de 0 à 10 ;
-  *Depression* : Niveau auto-évalué de dépression sur une échelle de 0 à 10 ;
-  *Insomnia* : Niveau auto-évalué d'insomnie sur une échelle de 0 à 10 ;
-  *OCD* : Niveau auto-évalué de troubles obsessionnels compulsifs (TOC) sur une échelle de 0 à 10 ;
-  *Âge* : Âge de l'individu ;
-  *BPM* : Rythme moyen (Battements par minute) des musiques écoutées, défini selon la fréquence d'écoute de chaque genre musical, défini entre 0 et 3 ;
-  *Music.effects* : L'effet auto-évalué des répondants de la musique sur leur santé mentale ;
-  *freq_lofi* : Fréquence d'écoute de musique lofi sur une échelle de 0 à 3 ;
-  *freq_metal* : Fréquence d'écoute de metal sur une échelle de 0 à 3 ;
-  *freq_classical* : Fréquence d'écoute de musique classique sur une échelle de 0 à 3 ;
-  *freq_rap* : Fréquence d'écoute de rap sur une échelle de 0 à 3 ;
-  *freq_hip_hop* : Fréquence d'écoute de musique hip-hop sur une échelle de 0 à 3 ;
-  *freq_pop* : Fréquence d'écoute de musique pop sur une échelle de 0 à 3 ;
-  *freq_rock* : Fréquence d'écoute de rock sur une échelle de 0 à 3.        

<hr style="height:2px; background-color:#333; border:none; margin-top:30px; margin-bottom:30px;">

## [📈 Statistiques descriptives :]{style="color: DarkSlateBlue;"}

Avant de passer à l’analyse économétrique, nous présentons ci-dessous quelques **statistiques descriptives** sur les principales *variables quantitatives* de notre base de données.

```{r, echo=FALSE, warning=FALSE, message=FALSE}
vars_quant <- df_final |> 
  select(Hours.per.day, Anxiety, Depression, Insomnia, OCD, Age, BPM)

stats_desc <- tibble(Variable = colnames(vars_quant),
                     Moyenne = sapply(vars_quant, mean),
                     Médiane = sapply(vars_quant, median),
                    `Écart-type` = sapply(vars_quant, sd),
                     Min = sapply(vars_quant, min),
                     Max = sapply(vars_quant, max))

stats_desc |> 
  kable(digits = 2,
        caption = "Tableau 1 : Statistiques descriptives des variables quantitatives",
        col.names = c("Variable", "Moyenne", "Médiane", "Écart-type", "Min", "Max")) |> 
  kable_styling(full_width = FALSE, bootstrap_options = c("striped", "hover","condensed"),
                position = "left")
```

--------------------------------------------------------------------------------

Afin de mieux comprendre la répartition des réponses, nous présentons ci-dessous les distributions des scores auto-évalués pour chacun des quatre troubles psychologiques étudiés : dépression, anxiété, insomnie et TOC.

```{r, echo=FALSE, warning=FALSE, message=FALSE}
df_long <- df_final |>
  pivot_longer(cols = c(Depression, Anxiety, Insomnia, OCD),
               names_to = "Trouble",
               values_to = "Score")

ggplot(df_long, aes(x = Score)) +
  geom_histogram(binwidth = 1, fill = "steelblue", color = "white") +
  facet_wrap(~ Trouble, ncol = 2) +
  labs(x = "Score (échelle de 0 à 10)",
       y = "Nombre d’individus",
       caption = "Figure 1 : Distributions des scores auto-évalués des troubles psychologiques") +
  theme_minimal(base_size = 13) +
  theme(plot.caption = element_text(hjust = 0.5, face = "bold", margin = margin(t = 15)),
        strip.text = element_text(face = "bold"))
```

--------------------------------------------------------------------------------

Enfin, pour clôturer cette partie descriptive, il est intéressant de se pencher sur la manière dont les participants perçoivent l’effet de la musique sur leur propre santé mentale.

```{r, echo = FALSE, warning=FALSE, message=FALSE}
avis_music <- df_final |>
  filter(Music.effects != "") |>
  count(Music.effects) |>
  mutate(Pourcentage_numerique = n / sum(n) * 100)

avis_music$Pourcentage_numerique <- round(avis_music$Pourcentage_numerique, 1)
ecart <- round(100 - sum(avis_music$Pourcentage_numerique), 1)
avis_music$Pourcentage_numerique[nrow(avis_music)] <- avis_music$Pourcentage_numerique[nrow(avis_music)] + ecart

avis_music <- avis_music |>
  mutate(Pourcentage = sprintf("%.1f %%", Pourcentage_numerique)) |>
  select(`Effet perçu` = Music.effects, Répondants = n, Pourcentage)

avis_music |>
  kable(
    caption = "Tableau 2 : Répartition des répondants selon leur perception de l'effet de la musique",
    align = "c",
    col.names = c("Effet perçu", "Nombre de répondants", "Pourcentage")) |>
  kable_styling(full_width = FALSE, bootstrap_options = c("striped", "hover", "condensed"), position = "left")
```

Nous avons une statistique intéressante, **75,7% estiment que la musique a un effet positif** sur leur santé mentale. Nous allons donc regarder si les résultats de l'étude sont en adéquation avec leurs avis.

<hr style="height:2px; background-color:#333; border:none; margin-top:30px; margin-bottom:30px;">


# [**Étude de données**]{style="color: Indigo;"}

## [🎧 Effet du temps d’écoute sur les troubles psychologiques :]{style="color: DarkSlateBlue;"}

Nous avons dans un premier temps évalué l’effet du nombre d’heures d’écoute musicale par jour (Hours.per.day) sur quatre variables psychologiques : l’**insomnie**, la **dépression**, l’**anxiété**, et les **troubles obsessionnels compulsifs** (OCD). Pour cela, nous avons réalisé quatre régressions linéaires simples, chacune avec un trouble comme variable dépendante.

--------------------------------------------------------------------------------

```{r, echo = FALSE, warning=FALSE, message=FALSE}
rls_insomnie <- lm(Insomnia ~ Hours.per.day, data=df_final)
if (interactive()) {summary(rls_insomnie)}
```

$Insomnia = \beta_0 + \beta_1 Hours.per.day + u$

```{r, echo=FALSE, results = 'asis'}
afficher_regression(rls_insomnie, "Tableau 3 : Régression linéaire simple sur l'insomnie en fonction du nombre d'heures d'écoute par jour")
```

Le lien entre le **temps d’écoute musicale** et la **dépression** est *significatif et positif*. Les individus qui écoutent davantage de musique tendent à déclarer des niveaux de dépression plus élevés. Ici encore, bien que le lien soit statistiquement significatif, *la part de variance expliquée est limitée* (R² ajusté = 0.01565).

--------------------------------------------------------------------------------

```{r, echo = FALSE, warning=FALSE, message=FALSE}
rls_depression <- lm(Depression ~ Hours.per.day, data=df_final)
if (interactive()) {summary(rls_depression)}
```

$Depression = \beta_0 + \beta_1 Hours.per.day + u$

```{r, echo=FALSE, results = 'asis'}
afficher_regression(rls_depression, "Tableau 4 : Régression linéaire simple sur la dépression en fonction du nombre d'heures d'écoute par jour")
```

Le lien entre le **temps d’écoute musicale** et la **dépression** est *significatif et positif*. Les individus qui écoutent davantage de musique tendent à déclarer des niveaux de dépression plus élevés. Ici encore, bien que le lien soit statistiquement significatif, *la part de variance expliquée est limitée* (R² ajusté = 0.01662).

--------------------------------------------------------------------------------

```{r, echo = FALSE, warning=FALSE, message=FALSE}
rls_anxiete <- lm(Anxiety ~ Hours.per.day, data=df_final)
if (interactive()) {summary(rls_anxiete)}
```

$Anxiety = \beta_0 + \beta_1 Hours.per.day + u$

```{r, echo=FALSE, results = 'asis'}
afficher_regression(rls_anxiete, "Tableau 5 : Régression linéaire simple sur l'anxiété en fonction du nombre d'heures d'écoute par jour")
```

Le **temps d’écoute quotidien** est *significativement et positivement* associé à l’**anxiété** (p < 0.05). Cela signifie que plus une personne écoute de la musique dans la journée, plus son score d’anxiété tend à être élevé. Toutefois, *la force de cette relation reste modérée* (R² = 0.00468), ce qui suggère que le temps d’écoute n’explique qu’une très faible partie de la variation de l’anxiété.

--------------------------------------------------------------------------------

```{r, echo = FALSE, warning=FALSE, message=FALSE}
rls_OCD <- lm(OCD ~ Hours.per.day, data=df_final)
if (interactive()) {summary(rls_OCD)}
```

$OCD = \beta_0 + \beta_1 Hours.per.day + u$

```{r, echo=FALSE, results = 'asis'}
afficher_regression(rls_OCD, "Tableau 6 : Régression linéaire simple sur les TOC en fonction du nombre d'heures d'écoute par jour")
```

Enfin, une association significative est aussi observée entre le **temps d’écoute** et les scores de **troubles obsessionnels compulsifs (TOC)**. Le modèle montre un *effet modéré* mais cohérent avec les résultats précédents, indiquant une tendance à des scores plus élevés en cas d’écoute prolongée.

--------------------------------------------------------------------------------

>🧠 **Interprétation globale :**
>
>Ces premiers résultats indiquent qu’il existe une <i>corrélation positive</i> entre le **temps passé à écouter de la musique chaque jour** et les **niveaux de quatre types de symptômes psychologiques**. Toutefois, il est important de souligner que la force des relations est faible (R² ajusté < 0.02), ce qui signifie que **d'autres variables non incluses dans ces modèles jouent un rôle plus important**.

<hr style="height:2px; background-color:#333; border:none; margin-top:30px; margin-bottom:30px;">


## [🎷	Notre style musical influencerait-il notre santé mentale ?]{style="color: DarkSlateBlue;"}

Après de nombreux échecs à tester des régressions avec différentes variables dont les effets n'étaient pas significatifs, nous sommes tombés sur une piste de recherche, le **niveau de dépression** en fonction de la **fréquence d'écoute de certains genres de musique**, tels que le *rock* ou le *métal*.

--------------------------------------------------------------------------------

### *[Régression linéaire sur la fréquence d'écoute du rock et du métal]{style="color: MidnightBlue;"}*

Nous testons d'abord l'effet de la **fréquence d'écoute du rock et du métal** sur le niveau de **dépression**.

```{r, echo = FALSE, warning=FALSE, message=FALSE}
rlm_rock_metal <- lm(Depression ~ freq_rock + freq_metal, data=df_final)
if (interactive()) {summary(rlm_rock_metal)}
```

$Depression = \beta_0 + \beta_1 \text{freq_rock} + \beta_2 \text{freq_metal} + u$

```{r, echo=FALSE, results = 'asis'}
afficher_regression(rlm_rock_metal, "Tableau 7 : Régression linéaire multiple sur la dépression en fonction de la fréquence d'écoute du rock et du métal")
```

Nous avons donc décidé d'approfondir cette piste de recherche en regroupant les genres *rock*, *métal*, *pop* et *hip-hop* dans une variable **freq_energique**, représente le niveau moyen d’écoute de musiques énergiques pour chaque répondant, sur une échelle de 0 à 3. Cette nouvelle variable fait donc la moyenne du niveau de réponse pour chaque genre inclus dans la variable. Nous avons fait pareil pour les musiques plus calmes comme le *classique* ou le *lofi* dans une variable nommée **freq_calme**.

--------------------------------------------------------------------------------

### *[Regroupement en deux groupes, fréquence énergique et fréquence calme]{style="color: MidnightBlue;"}*

```{r, echo = FALSE, warning=FALSE, message=FALSE}
df_final$freq_energique <- rowMeans(df_final[, c("freq_rock", "freq_metal", "freq_pop", "freq_hip_hop")], na.rm = TRUE)
df_final$freq_calme <- rowMeans(df_final[, c("freq_classical", "freq_lofi")], na.rm = TRUE)
```

```{r, echo = FALSE, warning=FALSE, message=FALSE}
rlm_energique <- lm(Depression ~ freq_energique + freq_calme, data = df_final)
if (interactive()) {summary(rlm_energique)}
```

$Depression = \beta_0 + \beta_1 \text{freq_energique} + \beta_2 \text{freq_calme} + u$

```{r, echo=FALSE, results = 'asis'}
afficher_regression(rlm_energique, "Tableau 8 : Régression linéaire multiple sur la dépression en fonction de la fréquence d'écoute de musiques énergiques et de musiques calmes")
```

On remarque avec cette régression, après avoir regroupé les genres musicaux selon qu'il s'agisse de genres calmes (classique, lofi) ou énergiques (rock, métal, pop, hip-hop), que **les musiques énergiques ont un effet significatif sur le niveau de dépression**. En moyenne, l'augmentation d'un échelon de la fréquence d’écoute de musiques énergiques entraîne l'augmentation de 1,2 échelon sur le niveau de dépression. À l'inverse, **l'effet des musiques calmes n'est pas significatif sur le niveau de dépression**, nous pouvons donc oublier cette variable.

--------------------------------------------------------------------------------

### *[Ajout de l'avis des répondants sur les effets de la musique]{style="color: MidnightBlue;"}*

Nous allons essayer d'inclure l'**avis subjectif** des répondants sur les **effets de la musique** sur leur santé mentale. La variable "Music.effects" peut prendre trois valeurs : "Worsen", "No effect" ou "Improve". Nous allons mettre *"No effect" en valeur de référence* pour nos régressions.

```{r, echo = FALSE, warning=FALSE, message=FALSE}
df_final$Music.effects <- relevel(as.factor(df_final$Music.effects), ref = "No effect")

rlm_music_effects <- lm(Depression ~ freq_energique + Music.effects, data = df_final)
if (interactive()) {summary(rlm_music_effects)}
```

$Depression = \beta_0 + \beta_1 \text{freq_energique} + \beta_2 \text{music.effects} + u$

```{r, echo=FALSE, results = 'asis'}
afficher_regression(rlm_music_effects, "Tableau 9 : Régression linéaire multiple sur la dépression en fonction de la fréquence d'écoute du rock et de l'avis sur l'effet de la musique")
```

```{r, tableau interprétation, echo=FALSE}
tibble(
  Variable = c("Intercept", "freq_energie", "Music.effectsImprove", "Music.effectsWorsen"),
  Estimate = c("2.48", "+1.18", "+0.47", "+2.75"),
  `p-value` = c("< 0.001", "< 0.001", "0.096", "< 0.001"),
  Significatif = c("✓", "✓", "✗", "✓"),
  Interprétation = c(
    "Score de dépression moyen pour une personne avec freq_energique = 0 et qui estime que la musique a \"no effect\".",
    "Chaque augmentation d’un point de la fréquence d’écoute de musiques énergiques est associée à +1.18 point de dépression en moyenne. (Effet significatif)",
    "Les individus qui estiment que la musique les aide (\"improve\") ont un score de dépression 0.47 points plus élevé que ceux qui disent \"no effect\", mais cet effet n'est significatif qu'à 10%.",
    "Ceux qui pensent que la musique empire leur état (\"worsen\") ont en moyenne +2.75 points de dépression par rapport à ceux qui disent \"no effect\". (Très significatif)"
  )) |> 
  kable(caption = "Tableau 10 : Interprétation des résultats de la régression",
        col.names = c("Variable", "Estimate", "p-value", "Significatif (à 5%)", "Interprétation")) |> 
  kable_styling(full_width = FALSE, bootstrap_options = c("striped", "hover", "condensed")) |> 
  column_spec(3, width = "6em") |> 
  column_spec(4, width = "10em", extra_css = "text-align: center;")
```

--------------------------------------------------------------------------------

### *[Ajout d'une interaction entre freq_energique et music.effects]{style="color: MidnightBlue;"}*

Dans la suite de cette analyse, nous allons tester si **l’impact de la musique énergique est plus fort** chez ceux qui disent qu'elle aggrave leur état grâce à **l'ajout d'une interaction** entre la variable *"freq_energique"* et *"music.effects"*.

```{r, echo = FALSE, warning=FALSE, message=FALSE}
inter_music_effects <- lm(Depression ~ freq_energique * Music.effects, data = df_final)
if (interactive()) {summary(inter_music_effects)}
```

$Depression = \beta_0 + \beta_1 \text{freq_energique} + \beta_2 \text{music.effects} + \beta_3 \text{freq_energique}*\text{music.effects} + u$

```{r, echo=FALSE, results = 'asis'}
afficher_regression(inter_music_effects, "Tableau 11 : Régression linéaire multiple avec interaction entre la fréquence d’écoute de musiques énergiques et l’effet perçu de la musique sur la dépression")
```

Les résultats montrent que :

- Pour les individus qui déclarent que *la musique n’a aucun effet sur leur santé mentale* (catégorie de référence), une augmentation d’**un point dans la fréquence d’écoute de musiques énergiques** est associée à une **hausse moyenne de 1.16 point sur l’échelle de dépression**.

- Pour ceux qui perçoivent *la musique comme aggravant leur état* (Worsen), l’interaction avec "freq_energique" est **positive et significative à 10%**. Cela suggère que pour ces individus, l’effet des musiques énergiques sur la dépression serait encore plus fort.

- À l’inverse, pour ceux qui *estiment que la musique les aide* (Improve), l’interaction avec "freq_energique" **n’est pas significative**, ce qui indiquerait que l’effet de la musique énergique est comparable à celui observé dans la catégorie de référence (No effect).

--------------------------------------------------------------------------------

>📌 **Interprétation globale :**
>
>Ces résultats suggèrent que l’impact des musiques énergiques sur le niveau de dépression **varie en fonction de la sensibilité subjective des individus** à la musique. Les effets sont les plus marqués chez ceux qui considèrent que la musique **aggrave leur état**, ce qui souligne l’importance d’intégrer les dimensions perceptives dans l’étude des liens entre musique et santé mentale.

--------------------------------------------------------------------------------

### *[Ajout de variables de contrôle telles que l'âge et le BPM]{style="color: MidnightBlue;"}*

Nous allons maintenant enrichir notre modèle en y ajoutant des **variables de contrôle** comme *l’âge des répondants* et *le BPM moyen des musiques écoutées*. Cela nous permet de tester si l’effet observé précédemment est robuste une fois ces facteurs pris en compte.

```{r, echo = FALSE, warning=FALSE, message=FALSE}
rlm_var_controle <- lm(Depression ~ freq_energique + Music.effects + Age + BPM, data=df_final)
if (interactive()) {summary(rlm_var_controle)}
```

$Depression = \beta_0 + \beta_1 \text{freq_energique} + \beta_2 \text{music.effects} + \beta_3 \text{âge} + \beta_4 BPM + u$

```{r, echo=FALSE, results = 'asis'}
afficher_regression(rlm_var_controle, "Tableau 12 : Régression linéaire multiple incluant des variables de contrôle (âge et BPM)")
```

**Pour conclure :**

- Bien que ces deux variables ne présentent **pas d'effet significatif** direct sur le niveau de dépression dans notre modèle, leur inclusion est essentielle pour éviter un biais **d’omission de variable**. Autrement dit, nous nous assurons que l'effet principal observé n’est pas simplement dû à l’influence cachée d’autres facteurs.

- Le résultat est clair : l'effet de la musique énergique reste **significatif** après l'ajout de ces contrôles, ce qui confirme la **solidité** et la **stabilité** du lien principal mis en évidence.

<hr style="height:2px; background-color:#333; border:none; margin-top:30px; margin-bottom:30px;">

## 🏆 [Choix du modèle préféré :]{style="color: DarkSlateBlue;"}

Après avoir testé plusieurs régressions simples et multiples, nous avons retenu comme modèle préféré la régression suivante :

$Depression = \beta_0 + \beta_1 \text{freq_energique} + \beta_2 \text{music.effects} + \beta_3 \text{âge} + \beta_4 BPM + u$

Ce modèle permet de prédire le niveau autoévalué de dépression en fonction de :

- la **fréquence d’écoute de musiques énergiques** (freq_energique) ; 

- la **perception subjective de l’effet de la musique** (Music.effects) ;

- deux **variables de contrôle** individuelles (Âge et BPM).

--------------------------------------------------------------------------------

**Raisons du choix :**

🎯 **Pertinence théorique :** le modèle prend en compte à la fois le comportement musical objectif (fréquence d’écoute par genre) et la perception subjective des effets de la musique, ce qui est central dans notre problématique.

🔍 **Robustesse :** l’effet de la musique énergique reste statistiquement significatif même après ajout de variables de contrôle, ce qui valide sa stabilité.

🧠 **Lisibilité :** contrairement au modèle avec interaction (plus complexe), ce modèle reste facile à interpréter et à expliquer, tout en intégrant plusieurs dimensions pertinentes.

📊 **Puissance explicative correcte :** bien que le R² ajusté reste modéré (~0.07), ce modèle explique davantage de variance que les régressions simples, tout en évitant le sur-ajustement.

<hr style="height:2px; background-color:#333; border:none; margin-top:30px; margin-bottom:30px;">

## 📌 [Questionnement sur l’endogénéité et les limites de notre base de données :]{style="color: DarkSlateBlue;"}

>⚠️️ L’ensemble des données utilisées dans notre analyse provient d’un questionnaire Google Forms rempli de manière volontaire et anonyme par des internautes. Ainsi, bien que la base soit riche et variée, elle **ne repose pas sur un échantillon aléatoire ou représentatif**, ce qui limite sa portée scientifique.  
>
> Notre objectif est de proposer une **analyse économétrique rigoureuse** à partir de ces données, tout en étant conscient des limites méthodologiques, en particulier des risques **d’endogénéité**, qui peuvent affecter la validité des résultats.

--------------------------------------------------------------------------------

### *[Problèmes potentiels d’endogénéité]{style="color: MidnightBlue;"}*

L’endogénéité survient lorsqu’une **ou plusieurs variables explicatives** sont **corrélées au terme d’erreur**, ce qui viole une des hypothèses fondamentales du modèle de régression linéaire. Voici **trois causes classiques** que nous devons garder à l’esprit :

- 🔁 **Causalité inverse** : il est possible que **l’écoute de musique influence la santé mentale**, mais également que **l’état de santé mentale d’un individu influence son temps d’écoute** (ex : une personne déprimée écoute plus de musique pour se réconforter).

- ❓ **Variable omise** : si des facteurs non observés comme des **troubles psychologiques passés, un environnement familial difficile ou des traits de personnalité** influencent à la fois la santé mentale et les habitudes musicales, cela crée un biais.

- 🎯 **Erreur de mesure** : les niveaux d’anxiété, dépression, insomnie ou TOC sont **autoévalués par les répondants**, sans standardisation. Il existe donc une **variabilité individuelle dans l’interprétation de l’échelle**, ce qui introduit une incertitude dans les mesures.

--------------------------------------------------------------------------------

### *[Pistes de solution : les variables instrumentales (VI)]{style="color: MidnightBlue;"}*

Une solution classique pour traiter l’endogénéité consiste à utiliser une **variable instrumentale**, qui permet d’isoler la partie “exogène” de la variable problématique.

Pour qu’une VI soit valide, elle doit :

- Être **corrélée à la variable explicative endogène** (ex : temps d’écoute ou fréquence de certains genres),
- Être **non corrélée au terme d’erreur**, c’est-à-dire n’avoir aucun effet direct sur la variable dépendante (la santé mentale), sauf via la variable explicative.

Dans notre cas, il est cependant difficile d’identifier une VI crédible à partir de la base de données disponible.

--------------------------------------------------------------------------------

### *[Autre limite : l’hétéroscédasticité]{style="color: MidnightBlue;"}*

Enfin, notre modèle pourrait également souffrir d’**hétéroscédasticité** : c’est-à-dire que la **variance de l’erreur n’est pas constante** selon les niveaux d’une variable indépendante.  
Par exemple, on pourrait supposer que **les effets de la musique sur la dépression varient plus fortement chez les jeunes que chez les plus âgés**, créant une plus grande dispersion des résidus dans certains sous-groupes.

<hr style="height:2px; background-color:#333; border:none; margin-top:30px; margin-bottom:30px;">

# [**Conclusion**]{style="color: Indigo;"}

À travers cette étude, nous avons tenté de répondre à la question suivante :  
**Dans quelle mesure les habitudes d’écoute musicale influencent-elles la santé mentale auto-déclarée chez les individus ?**

Nos analyses économétriques, basées sur une base de données volontaire et non représentative, ont mis en lumière plusieurs résultats intéressants :

- Une **corrélation positive** existe entre le **temps d’écoute quotidien** et différents troubles psychologiques (dépression, anxiété, insomnie, TOC), bien que les effets soient faibles.
- Les **genres musicaux écoutés** semblent également jouer un rôle. En particulier, la **fréquence d’écoute de musiques énergiques** (rock, métal, pop, hip-hop) est **significativement associée à un niveau plus élevé de dépression** autoévaluée.
- Ce lien est **modulé par l’effet perçu de la musique** : les personnes affirmant que la musique aggrave leur état sont celles pour qui cet effet est le plus marqué.
- L’ajout de **variables de contrôle** telles que l’âge ou le BPM moyen permet de **valider la robustesse** du lien principal, même si ces variables ne sont pas elles-mêmes significatives.

Malgré ces résultats, plusieurs **limites méthodologiques** subsistent, en particulier concernant l’**endogénéité potentielle**, l’**autoévaluation subjective des troubles** et l’**absence de représentativité de l’échantillon**.  
⚠️ Ces limites invitent à **interpréter les résultats avec prudence** et à envisager des études complémentaires, sur des données plus structurées et représentatives.

--------------------------------------------------------------------------------

En somme, ce projet montre que la musique, bien que bénéfique pour beaucoup, peut être vécue différemment selon les individus et leur état psychologique. Cela ouvre la voie à des pistes de réflexion intéressantes sur la **personnalisation de la musique comme outil thérapeutique ou préventif**.

<hr style="height:2px; background-color:#333; border:none; margin-top:30px; margin-bottom:30px;">